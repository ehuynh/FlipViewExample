== FlipView Example
A reminder for myself, but to share with anyone else that requires. This project shows example code to how to implement the ability to flip between two different views belonging to two different view controllers.